class Order {
    constructor(orderPrice, margin, ticker, orderType) {
        this.orderPrice = orderPrice
        this.margin = margin
        this.ticker = ticker
        this.orderType = orderType // 0 = Long, 1 = Short
    }

    sell() {
        if 
    }
}
